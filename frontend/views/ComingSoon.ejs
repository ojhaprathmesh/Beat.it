<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Beat.it - Feature Coming Soon">
    <title>Beat.it - Coming Soon</title>
    <link rel="shortcut icon" type="image/x-icon" href="/assets/browserIcon.ico" />

    <link rel="stylesheet" href="/styles/font.css">
    <link rel="stylesheet" href="/styles/colors.css">
    <link rel="stylesheet" href="/styles/modern-theme.css">
    
    <style>
        .coming-soon-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            text-align: center;
            padding: 2rem;
            position: relative;
            z-index: 10;
        }
        
        .coming-soon-logo {
            width: 120px;
            height: 120px;
            margin-bottom: 2rem;
        }
        
        .coming-soon-title {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: var(--text-primary);
        }
        
        .coming-soon-subtitle {
            font-size: 1.5rem;
            margin-bottom: 2rem;
            color: var(--text-secondary);
        }
        
        .coming-soon-text {
            max-width: 600px;
            margin-bottom: 2rem;
            color: var(--text-secondary);
            line-height: 1.6;
        }
        
        .animated-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
            overflow: hidden;
            background: linear-gradient(135deg, var(--bg-primary), var(--bg-secondary));
        }
        
        .animated-background::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 30% 50%, rgba(74, 108, 247, 0.3), transparent 70%),
                        radial-gradient(circle at 70% 70%, rgba(108, 92, 231, 0.3), transparent 70%);
            animation: pulse 8s infinite alternate;
        }
        
        @keyframes pulse {
            0% { opacity: 0.5; transform: scale(1); }
            100% { opacity: 0.8; transform: scale(1.1); }
        }
        
        .go-back-btn {
            padding: 12px 24px;
            background-color: var(--primary-color);
            color: white;
            border: none;
            border-radius: 6px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s ease;
            text-decoration: none;
            display: inline-block;
        }
        
        .go-back-btn:hover {
            background-color: var(--primary-color-dark);
            transform: translateY(-2px);
        }
    </style>
</head>

<body>
    <div class="animated-background"></div>
    
    <main class="coming-soon-container">
        <img src="/assets/Beat.it%20Logo.webp" alt="Beat.it" class="coming-soon-logo">
        <h1 class="coming-soon-title">Feature Coming Soon</h1>
        <h2 class="coming-soon-subtitle">We're working on something awesome!</h2>
        <p class="coming-soon-text">
            We're currently developing this feature to make your music experience even better. 
            Our team is working diligently to bring you innovative tools and capabilities.
            Check back soon to see what we've created!
        </p>
        <a href="javascript:history.back()" class="go-back-btn">Go Back</a>
    </main>

    <script src="/scripts/utility/themeUtils.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            // Initialize theme if available
            if (window.themeUtils) {
                window.themeUtils.initializeTheme();
            }
        });
    </script>
</body>

</html> 